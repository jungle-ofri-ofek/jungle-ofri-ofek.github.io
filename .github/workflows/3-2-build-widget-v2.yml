name: Build Jungle Widget v2
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Prepare source and key
        env:
          KEY_PASSWORD: ${{ secrets.JUNGLE_WIDGET_KEY_PASSWORD }}
        run: |
          rm -rf widget-src && mkdir widget-src
          unzip 1-jungle-widget-v2-source.zip -d widget-src
          openssl enc -d -aes-256-cbc -pbkdf2 -iter 200000 -in 3-jungle-release.keystore.enc -out widget-src/jungle-release.keystore -pass env:KEY_PASSWORD
      - name: Build release APK
        env:
          JUNGLE_STORE_PASSWORD: ${{ secrets.JUNGLE_WIDGET_KEY_PASSWORD }}
          JUNGLE_KEY_PASSWORD: ${{ secrets.JUNGLE_WIDGET_KEY_PASSWORD }}
        run: |
          cd widget-src
          gradle wrapper --gradle-version 8.9
          ./gradlew assembleRelease
          cp app/build/outputs/apk/release/app-release.apk ../1-jungle-widget-v2.apk
      - name: Verify APK
        run: |
          APKSIGNER=$(find "$ANDROID_HOME/build-tools" -name apksigner | sort -V | tail -1)
          AAPT=$(find "$ANDROID_HOME/build-tools" -name aapt | sort -V | tail -1)
          "$APKSIGNER" verify --verbose --print-certs 1-jungle-widget-v2.apk
          "$AAPT" dump badging 1-jungle-widget-v2.apk | head -20
          sha256sum 1-jungle-widget-v2.apk
      - uses: actions/upload-artifact@v4
        with:
          name: jungle-widget-v2
          path: 1-jungle-widget-v2.apk
